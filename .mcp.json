{
  "mcpServers": {
    "imap": {
      "title": "IMAP Plugin",
      "description": "Provider-independent local IMAP/SMTP connector with native local review for every change. Platform installers configure the matching launcher.",
      "cwd": ".",
      "command": "cmd.exe",
      "args": ["/d", "/s", "/c", "call", ".\\scripts\\launch.cmd"],
      "startup_timeout_sec": 20,
      "tool_timeout_sec": 360
    }
  }
}
